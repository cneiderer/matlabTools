function Etot=user1(theta_in,phi_in)
% Calculates total E-field pattern for user element as a function
% of theta and phi.
%
% Usage: Etot=user1(theta,phi)


global freq_config;
global user1_config;

% parameter1=user1_config(1,1);
% parameter2=user1_config(1,1);

% Etot= your function of theta_in and phi_in

Etot=1; % Default is isotropic